declare module '../platform/index.js' {
    const platform: {
        ALPHABET: {
            ALPHA_DIGIT: string;
        };
        generateString: (size: number, alphabet: string) => string;
    };
    export default platform;
} 